node_modules
.next
out
.DS_Store

# Secrets & environment — never commit real credentials (Turso, OpenAI, Blob, NextAuth, Archive, etc.)
.env
.env.*
!.env.example

# Keys & certificates
*.pem
*.key
*.p12
*.pfx
*.crt
*credentials*.json

npm-debug.log*
*.db
*.db-journal
drizzle/
uploads/
data/
